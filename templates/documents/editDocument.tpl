{strip}
<div class="card border-primary" id="editgroup">
    <div class="card-header bg-primary">{if $addDoc}Add{else}Edit{/if} Document</div>
    <div class="card-body">
        
    </div>
</div>
{/strip}